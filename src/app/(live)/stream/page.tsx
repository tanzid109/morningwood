import LiveStream from '@/Pages/LiveStream/LiveStream';
import React from 'react';

const page = () => {
    return (
        <div>
            <LiveStream/>
        </div>
    );
};

export default page;