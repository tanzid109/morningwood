import CreatorDashboard from '@/Pages/Dashboard/DashboardHome/CreatorDashboard';

const page = () => {
    return (
        <div>
            <CreatorDashboard/>
        </div>
    );
};

export default page;