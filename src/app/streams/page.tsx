import LiveStream from '@/Pages/Stream/LiveStream';
import React from 'react';

const page = () => {
    return (
        <div>
            <LiveStream/>
        </div>
    );
};

export default page;