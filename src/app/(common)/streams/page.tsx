import LiveStream from '@/pages/Streams/LiveStream';
import React from 'react';

const page = () => {
    return (
        <div>
            <LiveStream />
        </div>
    );
};

export default page;