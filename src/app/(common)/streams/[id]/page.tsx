import StreamPlayer from "@/components/StreamPlayer/StreamPlayer";

export const dynamic = 'force-dynamic';

const page = () => {
    return (
        <div>
            <StreamPlayer />
        </div>
    );
};

export default page;