import ChannelCustomize from '@/Pages/Dashboard/Customize/ChannelCustomize';
import React from 'react';

const page = () => {
    return (
        <div>
            <ChannelCustomize/>
        </div>
    );
};

export default page;