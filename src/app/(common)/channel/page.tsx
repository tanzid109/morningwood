import ChannelHome from "@/components/Channel/ChannelHome";


export const dynamic = 'force-dynamic';

const page = () => {
    return (
        <div>
            <ChannelHome />
        </div>
    );
};

export default page;