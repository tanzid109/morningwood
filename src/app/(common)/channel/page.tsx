import ChannelHome from '@/Pages/Channel/ChannelHome';
import React from 'react';

const page = () => {
    return (
        <div>
            <ChannelHome/>
        </div>
    );
};

export default page;