import LiveStreamPlayer from "@/components/LivePlayer/LivePlayer";

export const dynamic = 'force-dynamic';

const page = () => {
    return (
        <div>
            <LiveStreamPlayer />
        </div>
    );
};

export default page;